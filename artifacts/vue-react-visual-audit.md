# Vue-to-React visual parity audit

| Component | Status | Evidence |
| --- | --- | --- |
| AdvancedFilter | matched | Not applicable: no export, implementation, documentation demo, or renderable runtime surface exists; both imported Less files are comment-only placeholders that emit no CSS. |
| Alert | fixed | Vue `.arco-alert-icon` adapter now matches React icon sizing, alignment, spacing, and semantic colors. |
| Anchor | fixed | Paired basic, horizontal, static, lineless, affix, scroll-offset, and hash demos inspected. Vue renders the clickable title as `.arco-anchor-link` under `.arco-anchor-link-item` and uses `.arco-anchor-line-less`; adapters now match React's 4/8px padding, 14/22px type, 4px radius, hover/focus, active, and lineless-active tokens. |
| Avatar | fixed | React and Vue Avatar now share Starbucks semantic colors for the default background, text, trigger button, trigger hover, mask overlay, group border, and max-count avatar. Avatar demos replace Arco sample text and hardcoded Arco palette colors with Nink/Starbucks content and brand variables. |
| BackTop | fixed | Paired basic and custom-button scroll containers were scrolled past their thresholds. Vue uses `.arco-back-top`/`.arco-back-top-btn`; the visible button now matches React at 40×40px, white-on-brand colors, circular radius, 12px type, `--shadow-md`, and hover/active/focus-visible states. |
| Badge | fixed | Paired basic, standalone, dot, text, max, status, and color demos inspected. Shared number/dot geometry and typography already matched; Vue emits `normal`/`danger` status classes where React uses `default`/`error`, so exact Vue aliases now reuse React's neutral and danger dot tokens. |
| Breadcrumb | matched | Paired basic, custom separator/icon/size, ellipsis, parameterized, and dropdown demos inspected. Item, link, separator, last/current, ellipsis, and dropdown-trigger metrics/states already match; popup presentation is covered by the component-local Dropdown adapter. |
| Button | fixed | Paired basic, icon, shape, size, status, disabled, loading, long, and group demos inspected. Vue's `medium`/`only-icon` classes and `.arco-btn-icon > svg` nesting now map to React's 32px default control, 16px icon, and 24/32/40px icon-only widths. Arco Vue's existing medium icon wrapper supplies the single 8px leading-icon margin; the adapter adds no label or trailing-icon margin. |
| ButtonGroup | matched | Paired group demos inspected across primary, secondary, split-dropdown, round, icon-only, and mixed-content groups. Joined radii, -1px seams, border colors, stacking on hover/focus/active, and 32px member height already match; Button-owned Vue icon adapters remain scoped to Button. |
| Calendar | fixed | React and Vue Calendar now share the same full-width non-panel container contract while the card-calendar panel keeps its intrinsic panel width. Calendar and DatePicker popup panel metrics use the same header padding, body padding, week rhythm, 24px cell value size, and Starbucks selected/today/hover tokens. |
| Cascader | fixed | Vue Cascader and TreeSelect intentionally share the bare SelectView trigger contract in `Shared.less`; `.arco-select` is explicitly excluded. The shared 24/32/40px size, hover/focus/error/disabled rules and Cascader-owned 28px panel options match the accepted React visual contract. |
| Checkbox | fixed | Vue `.arco-checkbox-icon` was 14px with a 2px border and 2px radius; React's visible control is 16px with a 1px component border and 4px radius. Vue icon, label, checked, indeterminate, hover, and disabled selectors now use the React metrics/tokens. |
| Collapse | fixed | Paired basic, accordion, nested, borderless, custom, extra, icon-position, expand-icon, and destroy-on-hide states inspected. Vue owns horizontal content inset on `.arco-collapse-item-content`, with `.arco-collapse-item-content-box` adding only vertical padding; the adapter now produces React's combined 8/13/8/34px inset without doubling it. Header colors are limited to non-disabled headers so Arco's disabled state keeps precedence. |
| ColorPicker | matched | Paired default, mini, small, medium/default, large, and disabled demos match: 24/28/32/36px triggers, 1px component border, 4px radius, and 16/20/24/28px previews. React `default` and Vue `medium` class names already share the same rules. |
| DatePicker | matched | Paired rendered picker shells match at 32px height, 8px horizontal padding, 1px component/status border, 4px radius, 14/22px type, and shared disabled colors. Demo widths/status coverage differ where React/Vue source props differ; no Less mismatch was confirmed. |
| Descriptions | fixed | Paired basic, single-column, label alignment, bordered, layout, size, and responsive demos inspected. Vue's `medium` class is the React default-size contract; scoped rules now restore React's 16px title gap, 0/4/12/0px unbordered cell density, family/color, and bordered-shell radius without affecting other sizes. |
| Divider | matched | Paired horizontal, text-placement, and vertical demos inspected. Border direction, component divider color, text family, 14px type, and vertical metrics already match; no Vue adapter was added. |
| Drawer | fixed | Vue title/body/close-button portal classes and actual close-button hover state now inherit the React drawer metrics. |
| Dropdown | fixed | Paired basic, direction, disabled/divider, trigger, button, nested, controlled, group, context, and icon demos inspected with popups open. Vue's `.arco-dropdown`/`.arco-dropdown-option` DOM now matches React's 6px padding/radius, 0.5px component border, `--shadow-md`, 36px options with 12px inset, 2px row gap, hover/focus/active/disabled states, and group labels. Nested panels render as the same-node root `.arco-dropdown.arco-dropdown-submenu`, so they inherit the root panel rule without a descendant submenu selector. |
| Empty | fixed | Paired default and custom image/text empty states inspected. React nests image content under `.arco-empty-wrapper`, while Vue renders `.arco-empty-image` directly under the root; direct-child adapters now apply the same 6px image gap, disabled color, 48px icon metric, and 80px custom-image height. |
| FilterBar | matched | Not applicable: no export, implementation, documentation demo, or renderable runtime surface exists; both imported Less files are comment-only placeholders that emit no CSS. |
| Form | fixed | React and Vue Form now share the same horizontal label/control spacing, 24px item rhythm, no-wrap label contract, full-width demo form containers, and 16px form action spacing while preserving framework-native field validation and layout props. |
| Input | fixed | Vue's visible `.arco-input-wrapper` used an 8% white background, transparent border, 2px radius, and 16px horizontal padding; the adapter now matches React's container background, 1px component border, 4px radius, 8px padding, and focus/validation/disabled states. |
| InputNumber | fixed | Vue places the visible shell and state classes on `.arco-input-number.arco-input-wrapper`; it previously inherited the divergent Vue Input shell. The wrapper and button-mode inner/group surfaces now match React's 32px container, 8px padding, 1px component border, 4px radius, focus, and disabled tokens. |
| InputSearch | fixed | Vue composes search with an input wrapper, append slot, and `.arco-input-search-btn`; React uses a joined field/button shell. Vue selectors now remove the internal seam, apply joined 4px radii, zero append padding, and keep the search button at 32px. |
| InputTag | fixed | Vue calls the React default size `medium`; it now uses a 32px minimum height (without a fixed height), 2px vertical padding, 14/22px type, and wrapping draggable/overflow containers so additional tags can grow the field. |
| Link | fixed | Paired basic, status, icon, hover-background, and available loading demos inspected. Vue status classes now produce React colors for success `rgb(0,108,69)`, warning `rgb(149,69,0)`, danger `rgb(173,53,47)`, their hover/active/focus rings, disabled semantic colors, and the loading spinner/link color now stays on the Starbucks primary token while base/icon/hoverless metrics remain matched. |
| List | fixed | Paired basic, size, item/meta, actions, vertical, grid, responsive, scroll, and virtual/infinite demos inspected. Item density and separators already matched in equivalent size states; Vue's wrapper did not inherit the React design family/radius and its item main lacked an explicit shrink floor, so the root and main flow now use the React typography, 6px radius, and `min-width: 0` overflow contract. |
| Message | fixed | Vue message-list portal now matches React wrapper offsets, padding, and inline-flex message layout. |
| Modal | fixed | Vue body, close button including its actual hover state, default title alignment, and simple-modal body now match React. |
| Notification | fixed | Vue notification-list placement classes now use the React 20px edge offsets. |
| Pagination | fixed | Paired basic, more-pages, page-size, jumper, total, simple, size, buffer, custom button/style, and controlled demos inspected. Core 24/28/32/36px items and active/disabled states matched; Vue `.arco-pagination-options`, `.arco-pagination-total`, and `.arco-pagination-item-previous` now receive React flex, spacing, typography, size, hover, and disabled rules. |
| Popconfirm | fixed | Panel styling is scoped to Vue's popup shell; footer gap explicitly resets native button margin to prevent doubled spacing. |
| Popover | fixed | Panel styling is scoped to Vue's popup shell; inner content and popup arrow use React metrics. |
| Progress | fixed | Paired line, status, circle, mini, size, gradient, steps, and remaining-color demos inspected. Vue's `line-bar`, `circle-bg`/`circle-bar`, `size-mini`/`size-small`/`size-large`, and `status-warning`/`status-success`/`status-danger` classes now map to React track, brand, semantic, focus, and 12/20px, 14/22px, and 16/24px circle-text tokens without depending on React-only modifier names. |
| Radio | fixed | Vue renders `.arco-radio-icon`/`.arco-radio-label`, so React mask/text selectors did not apply. Vue icon, label, hover, checked-dot, and disabled adapters now match the React 16px control, 1px secondary-component border, 8px label gap, and state tokens. |
| Select | fixed | React/Vue `select/multi-select` and `select/select-size` inspected at 1440×1000: Select-scoped `.arco-select.arco-select-view-multiple` and multiple-content popup adapters match React container (32px, 3/8/3/4px), hover/focus, 24px tags, 16px suffix, 28px options, selected/active/disabled and searchable-empty popup states, overflow, and mini/small/medium/large mappings. Form error/warning DOM and the available single-select loading demo were also checked without extending the adapters to other components. |
| SelectCard | matched | Not applicable: no export, implementation, documentation demo, or renderable runtime surface exists; both imported Less files are comment-only placeholders that emit no CSS. |
| Skeleton | fixed | Paired basic and animation states plus each framework's available line/image/shape states inspected. Vue's `line-row`/`shape` nodes receive React's secondary fill, 16px line height, 6px radius, and 12px row gap; `.arco-skeleton-animation` now applies the React gradient tokens directly to those Vue-owned nodes. |
| Slider | fixed | React `.arco-slider-road/button` and Vue `.arco-slider-track/btn` now share the same Starbucks semantic colors for the neutral track, brand active bar, handle border, hover/active shadow, focus ring, active dots/ticks, disabled states, and mark text. |
| Spin | fixed | Vue's loading mask now uses the same translucent container overlay as React. |
| Statistic | fixed | React and Vue Statistic now share Starbucks semantic typography and colors for root text, title, value, prefix/suffix icons, decimal/integer spans, and extra content while preserving Arco's content flow so `extra` renders below the value. Statistic demos replace hardcoded green/red Arco-style values with success and danger CSS variables and widen the animated-stat columns to avoid overlap. |
| Steps | fixed | Paired basic, mini, description, custom icon/node, placement, error, lineless, vertical, arrow, dot, navigation, and switchable demos inspected. Default/small metrics, process/finish/wait/error colors, connectors, arrow/dot/navigation modes already match; Vue `.arco-steps-changeable` now receives React's non-active hover title/description color. |
| Switch | fixed | The root track already matched, but Vue uses `.arco-switch-handle` where React uses its dot class. The handle now matches the supported default 12px, small 10px, checked 15/12px, round, and disabled metrics. Unsupported `.arco-switch-large` rules were removed because Vue exposes no large size. |
| Table | fixed | Equivalent basic, selection, expand/tree, sort/filter, render, ellipsis, fixed/scroll, grouped, merged, editable, resize, sticky, draggable, virtual, empty, and summary demos inspected. Vue names the React default density `large` and owns its padding on child `.arco-table-cell`; only that node receives React's 9px/20px inset, leaving `.arco-table-td` and native table scroll containers untouched. |
| Tabs | fixed | Paired line, icon, placement, variants, extra, dynamic/card, controlled, nested/vertical, size, scroll, lazy, and trigger demos inspected. Vue renders `.arco-tabs-tab`/`-active`/`-disabled` and `.arco-tabs-nav-ink`; exact base and `:hover` adapters now have sufficient specificity for React's secondary/primary-hover colors, while active/disabled, brand ink, and focus states keep precedence. |
| Tag | matched | Paired basic, closable, selectable, color, dynamic, size, icon, loading, and bordered demos inspected. The shared root, size, checked/color, icon, close, hover, and bordered selectors already apply React tokens to Vue DOM; no adapter was required. |
| TagGroup | matched | Not applicable: graph-backed code search found no export, implementation, documentation demo, or runtime surface in either framework; both imported Less files remain identical comment-only placeholders. |
| TimePicker | matched | Paired basic, prefix/range, disabled, and custom/default-value shells match at 32px height, 8px horizontal padding, 1px component border, 4px radius, 14/22px type, and shared disabled colors. Status/size demo props differ between frameworks; no Less mismatch was confirmed. |
| Timeline | fixed | Shared item, label, content, dot, connector, relative-label, and horizontal-spacing selectors already match. Timeline demos now use Starbucks store/drink milestones, token colors, and local coffee-type icon nodes instead of Byte/Arco product logos and placeholder milestone text. |
| ToolBar | matched | Not applicable: neither package exports or implements a ToolBar component and the paired override files are identical comment-only placeholders that emit no CSS; no documentation demo or renderable runtime surface exists. |
| Tooltip | fixed | Vue popup arrow now follows the React tooltip panel and custom-background behavior. |
| Tree | fixed | Figma node 2374:24641 and its hover, active, disabled, and checkable variants were inspected. React and Vue now share the 34px medium row, 8px first-level inset, 24px hierarchy step, 16px switcher target with an 8px caret, 14px/22px title, 4px radius, secondary-container hover, brand-light selected state, disabled treatment, checkbox spacing, connector colors, RTL spacing, and dark-mode token mapping. |
| Upload | matched | Equivalent basic, disabled, file-list, custom-icon, and avatar surfaces retain the same shared tokens and control metrics. The drag demos are not equivalent: React supplies `drag`, multiple/accept/tip/message content while Vue only supplies `draggable` and `action`, explaining their different content height without a Less defect. |
| VerificationCode | fixed | React and Vue now scope their VerificationCode cells to the shared Input interaction contract: 30px default cells, 24px mini/small cells, 38px large cells, centered text, 4px gaps, 4px radius, and matching hover/focus/error/disabled tokens. |
