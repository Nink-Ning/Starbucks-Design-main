---
import { renderLandingPage } from '../landing/renderLandingPage';

export const prerender = true;

return new Response(renderLandingPage(import.meta.env.BASE_URL), {
  headers: {
    'Content-Type': 'text/html; charset=utf-8',
  },
});
---
